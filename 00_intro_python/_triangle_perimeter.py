def main():
    side1 : float = float(input("what is the value of side1?"))
    side2 : float = float(input("what is the value of side2?"))
    side3 : float = float(input("what is the value of side3?"))

    total = (side1 + side2 + side3)
    print("the perimeter of all sides of triangle is" + str(side1 + side2 + side3))
main()